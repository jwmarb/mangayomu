import { StyleSheet } from 'react-native';

export const iconButton = StyleSheet.create({
  container: {
    width: 56,
    height: 56,
    overflow: 'hidden',
    borderRadius: 10000,
  },
  containerSmall: {
    width: 36,
    height: 36,
    overflow: 'hidden',
    borderRadius: 10000,
  },
  pressable: {
    justifyContent: 'center',
    alignItems: 'center',
  },
});

import { Pressable } from 'react-native';
import Animated from 'react-native-reanimated';

const AnimatedNativePressable = Animated.createAnimatedComponent(Pressable);

export default AnimatedNativePressable;

import { rem } from '@theme/Typography';
import styled, { css } from 'styled-components/native';

export const MangaSourceIcon = styled.Image`
  ${(props) => css`
    width: ${rem(32)};
    height: ${rem(32)};
  `}
`;

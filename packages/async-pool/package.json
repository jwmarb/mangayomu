{
  "name": "@mangayomu/async-pool",
  "version": "1.0.0",
  "description": "A library to customize concurrency for Promises",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "build": "tsc --build",
    "reset": "rm -rf node_modules dist coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  }
}
